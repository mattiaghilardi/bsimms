% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{waic.bsimms_fit}
\alias{waic.bsimms_fit}
\title{Widely applicable information criterion (WAIC)}
\usage{
\method{waic}{bsimms_fit}(x, ...)
}
\arguments{
\item{x}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{...}{Further arguments passed on to \code{\link[loo:waic.array]{loo::waic.array()}}.}
}
\value{
A \code{waic} object, as returned by \code{\link[loo:waic]{loo::waic()}}.
}
\description{
Computes WAIC (Watanabe 2010) from the model's \code{log_lik} generated
quantity (see \code{\link[=loo.bsimms_fit]{loo.bsimms_fit()}} for what constitutes a leave-one-out
unit here). \code{\link[=loo.bsimms_fit]{loo.bsimms_fit()}} is recommended over WAIC since PSIS-LOO
additionally provides Pareto k reliability diagnostics; WAIC is provided
mainly for comparison with WAIC values reported by other mixing-model
software.
}
\examples{
\donttest{
sim <- simulate_bsimms_data(
  ~1,
  n_mixture_obs = 10,
  source_names = c("Beaver", "Deer", "Hare"),
  isotope_names = c("d13C", "d15N"),
  seed = 1
)
fit <- bsimm(
  sim$formula, mixture_data = sim$mixture_data,
  source_data = sim$source_data, tdf_data = sim$tdf_data,
  isotope_names = sim$isotope_names,
  source_means_sds = sim$source_means_sds, tdf_means_sds = sim$tdf_means_sds,
  conc_dep = sim$conc_dep, error_structure = sim$error_structure,
  source_col = sim$source_col,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
loo::waic(fit)
}
}
\references{
Watanabe, S. (2010). Asymptotic equivalence of Bayes cross
validation and widely applicable information criterion in singular
learning theory. \emph{Journal of Machine Learning Research}, 11, 3571-3594.
}
