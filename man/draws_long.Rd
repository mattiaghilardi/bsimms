% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws.R
\name{draws_long}
\alias{draws_long}
\title{Reshape a draws array into long format}
\usage{
draws_long(arr, var_col = "variable", value_col = "value")
}
\arguments{
\item{arr}{A numeric \verb{[n_draws, n_obs, n_var]} array, with variable
names attached as the 3rd dimension's \code{dimnames}.}

\item{var_col}{Name to give the variable-identity column (default
\code{"variable"}), e.g. \code{"source"} for a \code{\link[=posterior_proportions]{posterior_proportions()}} array
or \code{"isotope"} for a \code{\link[=posterior_epred.bsimms_fit]{posterior_epred.bsimms_fit()}} array.}

\item{value_col}{Name to give the value column (default \code{"value"}).}
}
\value{
A long-format data frame with columns \code{draw} (posterior draw
index), \code{row} (observation index, into \code{newdata} or the fitted
mixture samples), \verb{<var_col>}, and \verb{<value_col>}.
}
\description{
Reshapes a \verb{[n_draws, n_obs, n_var]} array (as returned by
\code{\link[=posterior_proportions]{posterior_proportions()}}, \code{\link[=posterior_epred.bsimms_fit]{posterior_epred.bsimms_fit()}}, or
\code{\link[=posterior_predict.bsimms_fit]{posterior_predict.bsimms_fit()}}), with variable names attached as the
3rd dimension's \code{dimnames}, into a long-format data frame with one row
per (draw, observation, variable) triple, ready for custom plots or
summaries (e.g. with \code{ggplot2}/\code{ggdist}).
}
\examples{
\donttest{
sim <- simulate_bsimms_data(
  ~1,
  n_mixture_obs = 10,
  source_names = c("Beaver", "Deer", "Hare"),
  isotope_names = c("d13C", "d15N"),
  seed = 1
)
fit <- bsimm(
  sim$formula, mixture_data = sim$mixture_data,
  source_data = sim$source_data, tdf_data = sim$tdf_data,
  isotope_names = sim$isotope_names,
  source_means_sds = sim$source_means_sds, tdf_means_sds = sim$tdf_means_sds,
  conc_dep = sim$conc_dep, error_structure = sim$error_structure,
  source_col = sim$source_col,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
p_arr <- posterior_proportions(fit)
draws_long(p_arr, var_col = "source", value_col = "proportion")
}
}
