% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_bsimms_data}
\alias{simulate_bsimms_data}
\title{Simulate data for a \code{bsimms} model}
\usage{
simulate_bsimms_data(
  formula = ~1,
  n_mixture_obs,
  n_sources = 3,
  source_names = NULL,
  n_isotopes = 2,
  isotope_names = NULL,
  n_levels = list(),
  n_groups = list(),
  balanced = TRUE,
  source_means_sds = FALSE,
  n_source_obs = 10,
  tdf_means_sds = TRUE,
  n_tdf_obs = 10,
  conc_dep = FALSE,
  error_structure = c("process_residual", "process_only", "residual_only"),
  p_global = NULL,
  sigma = NULL,
  resid_prop = NULL,
  source_col = "Source",
  seed = NULL
)
}
\arguments{
\item{formula}{A one-sided \code{lme4}-style formula, as in \code{\link[=bsimm]{bsimm()}}. Random
slopes (e.g. \code{(x | Group)}) are not supported.}

\item{n_mixture_obs}{Number of mixture observations to simulate.}

\item{n_sources}{Number of sources to simulate. Ignored if \code{source_names}
is supplied.}

\item{source_names}{Optional character vector of source names. If
\code{NULL} (default), \code{n_sources} sources named \code{"source1"}, \code{"source2"},
... are used.}

\item{n_isotopes}{Number of isotopes to simulate. Ignored if
\code{isotope_names} is supplied.}

\item{isotope_names}{Optional character vector of isotope names. If
\code{NULL} (default), \code{n_isotopes} isotopes named \code{"isotope1"},
\code{"isotope2"}, ... are used.}

\item{n_levels}{Named list, one entry per fixed-effect factor in
\code{formula}, giving that factor's number of levels (an integer >= 2).
Any variable in \code{formula} not listed here is generated as a continuous
covariate instead.}

\item{n_groups}{Named list, one entry per random-effect grouping factor
in \code{formula} (i.e. every variable referenced to the right of \code{|}),
giving that factor's number of groups (an integer >= 2). Required for
every grouping factor in \code{formula}.}

\item{balanced}{Logical; split \code{n_mixture_obs} as evenly as possible
across each factor's/grouping factor's levels (\code{TRUE}, default; sizes
differ by at most one observation when \code{n_mixture_obs} is not a
multiple of the number of levels), or assign them at random (\code{FALSE})?
Either way every level/group gets at least one observation.}

\item{source_means_sds, tdf_means_sds}{Logical; generate \code{source_data}/
\code{tdf_data} as means/SDs (\code{TRUE}) or raw replicate samples (\code{FALSE})?
Default \code{FALSE} for \code{source_means_sds}, \code{TRUE} for \code{tdf_means_sds},
matching \code{\link[=bsimm]{bsimm()}}'s own defaults.}

\item{n_source_obs}{Number of raw replicate observations to simulate per
source: either a single integer, recycled to every source (default
10), or a numeric vector of length \code{n_sources} giving each source its
own count (allowing unbalanced source data). Ignored if
\code{source_means_sds = TRUE}.}

\item{n_tdf_obs}{As \code{n_source_obs}, for \code{tdf_data}. Ignored if
\code{tdf_means_sds = TRUE}.}

\item{conc_dep}{Logical; also generate a \verb{<isotope>_conc} column (in
\verb{(0, 1]}, summing to at most 1 across isotopes for a given source) in
\code{source_data} for every isotope, enabling concentration dependence
(default \code{FALSE})?}

\item{error_structure}{One of \code{"process_residual"} (default: source/TDF
variance propagated into the mixture and scaled by an estimated
multiplicative residual-error factor, i.e. the "Residual * Process"
error structure of Stock & Semmens 2016), \code{"process_only"} (only
source/TDF variance propagated, no separate residual term, as in the
original MixSIR model, Moore & Semmens 2008), or \code{"residual_only"}
(source/TDF means treated as fixed and known; all unexplained
variance goes into a single residual error term, as in the original
SIAR model, Parnell et al. 2010).}

\item{p_global}{Optional numeric vector of length \code{n_sources}, the true
baseline source proportions (must sum to 1). If \code{NULL} (default),
randomly generated.}

\item{sigma}{Optional numeric vector of length \code{n_isotopes}, the true
residual SD for each isotope. Used only when \code{error_structure = "residual_only"} (ignored otherwise). If \code{NULL} (default), randomly
generated.}

\item{resid_prop}{Optional numeric vector of length \code{n_isotopes}, the
true residual-error factor scaling process variance for each isotope.
Used only when \code{error_structure = "process_residual"} (ignored
otherwise). If \code{NULL} (default), randomly generated.}

\item{source_col}{Name of the source-identifier column shared by
\code{source_data} and \code{tdf_data}. Default \code{"Source"}.}

\item{seed}{Optional integer seed. The global RNG state is saved and
restored, so calling this function does not affect the caller's own
random draws.}
}
\value{
A list with elements \code{formula}, \code{mixture_data}, \code{source_data},
\code{tdf_data}, \code{isotope_names}, \code{source_means_sds}, \code{tdf_means_sds},
\code{conc_dep}, \code{error_structure}, \code{source_col} (in the same order as
\code{\link[=bsimm]{bsimm()}}'s arguments, ready to pass to it), and \code{truth}, the true
generating values that \code{\link[=bsimm]{bsimm()}} would later estimate from the data:
\code{p_global}, \code{fixed} (matrix of fixed-effect ILR-space coefficients, if
\code{formula} has fixed-effect terms), \code{random} (list of group-level
SDs/realised effects, one element per random-effect term, if any),
\code{source_mean}/\code{source_sd} (if \code{source_means_sds = FALSE}),
\code{tdf_mean}/\code{tdf_sd} (if \code{tdf_means_sds = FALSE}), and \code{sigma} or
\code{resid_prop} (whichever applies to \code{error_structure}).
}
\description{
Generates \code{mixture_data}, \code{source_data}, and \code{tdf_data} for an arbitrary
\code{bsimms} model configuration (any number of sources/isotopes, raw or
summarised source/TDF data, any error structure, with or without
concentration dependence, and any \code{lme4}-style fixed/random-effects
\code{formula} of mixture-level covariates), together with the known
generating ("true") parameter values. Useful for testing \code{bsimm()}
across configurations, and for checking parameter recovery.
}
\details{
Source proportions and mixture isotope values are simulated forward from
known truth: a baseline \code{p_global}, formula-driven fixed/random-effect
deviations from it in ILR space, source and TDF isotope means/SDs, and
observation-level noise appropriate to \code{error_structure}. Random-effect
terms in \code{formula} are currently limited to random intercepts (\code{(1 | Group)}, including crossed/nested grouping factors); random slopes are
not yet supported.

Every covariate referenced in \code{formula} must be either a fixed-effect
factor named in \code{n_levels}, a random-effect grouping factor named in
\code{n_groups}, or otherwise is generated as an independent standard normal
(continuous) covariate. Source means are generated to be well-separated
and non-collinear in isotope space (sampled without replacement from a
pool of \code{2 * n_sources} equally spaced candidate values per isotope,
redrawn if the resulting configuration is collinear), rather than
sampled independently from a probability distribution, which cannot
guarantee isotopically distinct sources.

A warning is issued when \code{n_sources > n_isotopes + 1}: the mixing system
is then underdetermined in the classical sense (more sources than
isotopes can resolve without help from the prior), which \code{bsimms}
supports but which relies more heavily on the prior than the data.
}
\examples{
sim <- simulate_bsimms_data(
  ~ Sex + (1 | Region),
  n_mixture_obs = 60,
  n_levels = list(Sex = 2),
  n_groups = list(Region = 3)
)
str(sim, max.level = 1)
}
\references{
Moore, J.W., & Semmens, B.X. (2008). Incorporating
uncertainty and prior information into stable isotope mixing models.
\emph{Ecology Letters}, 11(5), 470-480.
\doi{10.1111/j.1461-0248.2008.01163.x}

Parnell, A.C., Inger, R., Bearhop, S., & Jackson, A.L.
(2010). Source partitioning using stable isotopes: coping with too
much variation. \emph{PLoS ONE}, 5(3), e9672.
\doi{10.1371/journal.pone.0009672}

Stock, B.C., & Semmens, B.X. (2016). Unifying error
structures in commonly used biotracer mixing models. \emph{Ecology},
97(10), 2562-2569. \doi{10.1002/ecy.1517}
}
