% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes_R2.R
\name{bayes_R2.bsimms_fit}
\alias{bayes_R2.bsimms_fit}
\title{Bayesian R-squared}
\usage{
\method{bayes_R2}{bsimms_fit}(
  object,
  resp = NULL,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  ...
)
}
\arguments{
\item{object}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{resp}{Optional character vector of isotope names (a subset of
\code{isotope_names}) to compute R-squared for. \code{NULL} (default) computes it for
every isotope.}

\item{summary}{Logical; return a summary data frame (default) or the raw
posterior draws.}

\item{robust}{Logical; if \code{FALSE} (default) summarise the central
tendency/spread with \code{mean}/\code{sd}, if \code{TRUE} use \code{median}/\code{mad} instead.}

\item{probs}{Quantiles to include in the summary (default 2.5\%/97.5\%).}

\item{...}{Currently unused.}
}
\value{
If \code{summary = TRUE}, a data frame with one row per isotope
(\code{isotope}, and one column per summary measure, named as in
\code{\link[posterior:summarise_draws]{posterior::summarise_draws()}}: \code{mean}, \code{sd}, \code{q2.5}, \code{q97.5}, or with
\code{robust = TRUE}, \code{median}, \code{mad}, \code{q2.5}, \code{q97.5}). If \code{FALSE}, an
\verb{n_draws x length(resp)} matrix of R-squared draws (column names = isotope
names).
}
\description{
Computes a Bayesian R-squared (Gelman, Goodrich, Gabry, and Vehtari 2019)
for one or more isotopes. If \code{object$criteria$bayes_R2} was already
cached via \code{\link[=add_criterion]{add_criterion()}}, it is reused (subset to \code{resp}) instead of
recomputed.
}
\examples{
\donttest{
sim <- simulate_bsimms_data(
  ~Sex,
  n_mixture_obs = 10,
  source_names = c("Beaver", "Deer", "Hare"),
  isotope_names = c("d13C", "d15N"),
  n_levels = list(Sex = 2),
  seed = 1
)
fit <- bsimm(
  sim$formula, mixture_data = sim$mixture_data,
  source_data = sim$source_data, tdf_data = sim$tdf_data,
  isotope_names = sim$isotope_names,
  source_means_sds = sim$source_means_sds, tdf_means_sds = sim$tdf_means_sds,
  conc_dep = sim$conc_dep, error_structure = sim$error_structure,
  source_col = sim$source_col,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
rstantools::bayes_R2(fit)
}
}
\references{
Gelman, A., Goodrich, B., Gabry, J., & Vehtari, A. (2019).
R-squared for Bayesian regression models. \emph{The American Statistician},
73(3), 307-309. \doi{10.1080/00031305.2018.1549100}.
(\href{https://acris.aalto.fi/ws/portalfiles/portal/34206843/bayes_R2_v3.pdf}{Preprint})
}
