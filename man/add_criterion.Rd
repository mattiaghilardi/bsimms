% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{add_criterion}
\alias{add_criterion}
\alias{add_criterion.bsimms_fit}
\title{Cache LOO/WAIC/R-squared criteria on a \code{bsimms} fit}
\usage{
add_criterion(x, ...)

\method{add_criterion}{bsimms_fit}(x, criterion = "loo", ...)
}
\arguments{
\item{x}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{...}{Further arguments passed on to \code{\link[=loo.bsimms_fit]{loo.bsimms_fit()}} (if
\code{"loo"} is requested), \code{\link[=waic.bsimms_fit]{waic.bsimms_fit()}} (\code{"waic"}), or
\code{\link[=bayes_R2.bsimms_fit]{bayes_R2.bsimms_fit()}} (\code{"bayes_R2"}, always cached with
\code{summary = FALSE}, i.e. the raw posterior draws).}

\item{criterion}{Character vector of criteria to compute and cache:
\code{"loo"} (default), \code{"waic"}, \code{"bayes_R2"}, or any combination.}
}
\value{
\code{x}, with \code{x$criteria} updated to include the newly computed
criterion/criteria.
}
\description{
Computes one or more model-evaluation criteria and stores them in
\code{x$criteria} (a named list), returning the augmented fit.
\code{\link[=loo_compare.bsimms_fit]{loo_compare.bsimms_fit()}} reuses a cached \code{"loo"}/\code{"waic"} criterion
automatically instead of recomputing it; \code{"bayes_R2"} is cached purely
for reuse/comparison.
}
