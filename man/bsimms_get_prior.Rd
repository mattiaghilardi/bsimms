% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{bsimms_get_prior}
\alias{bsimms_get_prior}
\title{Default priors for a \code{bsimms} model}
\usage{
bsimms_get_prior(
  formula,
  mixture_data,
  source_data,
  tdf_data,
  isotope_names,
  source_means_sds = FALSE,
  tdf_means_sds = TRUE,
  conc_dep = FALSE,
  error_structure = c("process_residual", "process_only", "residual_only"),
  source_col = "Source"
)
}
\arguments{
\item{formula}{A one-sided \code{lme4}-style formula for the source
proportions, e.g. \code{~ Sex + Season + (1 + Season | Region)}. All
variables referenced must be columns of \code{mixture_data}. The
population-level baseline is not part of \code{formula}: it is \code{p_global},
a \code{simplex[K]} parameter with a \code{Dirichlet(alpha)} prior (as in
\code{MixSIAR}), and \code{formula}'s terms are covariate deviations from it,
added in ILR space (see \code{\link[=bsimms_prior]{bsimms_prior()}}'s \code{"p_global"} class to set
\code{alpha}). A formula with no covariates (e.g. \code{~ 1}) is a valid
Dirichlet-baseline-only model.}

\item{mixture_data}{Data frame of mixture observations (e.g. consumers,
in a diet-mixing study): one row per individual (or per sample), with
a column for each entry of \code{isotope_names}, plus any
covariate/grouping columns used in \code{formula}.}

\item{source_data}{Source isotope data. If \code{source_means_sds = FALSE}
(default), long format with one row per raw sample and columns
\code{Source}, \code{isotope_names}. If \code{source_means_sds = TRUE}, one row per
source with columns \code{Source}, \verb{<isotope>_mean}, \verb{<isotope>_sd} for
every isotope.}

\item{tdf_data}{Trophic discrimination factor (diet-tissue discrimination)
data, same layout convention as \code{source_data}, controlled by
\code{tdf_means_sds} (default \code{TRUE}, since TDFs are usually taken from the
literature as means/SDs).}

\item{isotope_names}{Character vector of isotope column names shared by
\code{mixture_data}, \code{source_data} and \code{tdf_data}, e.g. \code{c("d13C", "d15N")}.}

\item{source_means_sds}{Logical; is \code{source_data} supplied as
means/SDs (\code{TRUE}) or raw replicate samples (\code{FALSE}, default)? When
raw, source means and SDs are estimated as part of the model, and
their uncertainty is fully propagated into the posterior.}

\item{tdf_means_sds}{Logical; is \code{tdf_data} supplied as means/SDs
(\code{TRUE}, default) or raw replicate samples (\code{FALSE})? When raw, TDF
means and SDs are estimated as part of the model and their uncertainty
is fully propagated into the posterior. Unlike raw source data, though,
no cross-isotope correlation is estimated for raw TDF data (there is
no \code{"tdf_cor"} class in \code{\link[=bsimms_prior]{bsimms_prior()}}): the mixture likelihood only
takes the multivariate (\code{multi_normal_cholesky}) form when \emph{source}
data is raw and there are 2+ isotopes. This asymmetry is intentional
(raw TDF data with enough replicates per source to usefully estimate a
correlation is a rare combination in practice), not an oversight, but
could be added symmetrically to \code{source_cor} if needed.}

\item{conc_dep}{Logical; enable elemental concentration dependence
(default \code{FALSE})? If \code{TRUE}, \code{source_data} must have a \verb{<isotope>_conc}
column for every isotope in \code{isotope_names}, giving that source's
proportional elemental concentration (in \verb{(0, 1]}) for that isotope's
element -- one value per raw sample (averaged per source) if
\code{source_means_sds = FALSE}, or one value per source if
\code{source_means_sds = TRUE}. Since these are proportions of the same
source's total mass, they cannot sum to more than 1 across isotopes.}

\item{error_structure}{One of \code{"process_residual"} (default: source/TDF
variance propagated into the mixture and scaled by an estimated
multiplicative residual-error factor, i.e. the "Residual * Process"
error structure of Stock & Semmens 2016), \code{"process_only"} (only
source/TDF variance propagated, no separate residual term, as in the
original MixSIR model, Moore & Semmens 2008), or \code{"residual_only"}
(source/TDF means treated as fixed and known; all unexplained
variance goes into a single residual error term, as in the original
SIAR model, Parnell et al. 2010).}

\item{source_col}{Name of the source-identifier column shared by
\code{source_data} and \code{tdf_data}. Default \code{"Source"}.}
}
\value{
A \code{bsimms_prior} data frame.
}
\description{
Builds the model specification (design matrices, source/TDF data layout)
without generating or fitting the Stan model, and returns the table of
default priors that would be used. Edit
rows of the result and pass to the \code{prior} argument of \code{make_stancode()},
\code{make_standata()} or \code{bsimm()} to override.
}
\examples{
mixture_data <- data.frame(
  d13C = c(-20, -21, -19, -22, -20.5, -21.5),
  d15N = c(10, 11, 9, 12, 10.5, 11.5),
  Region = factor(rep(c("A", "B"), each = 3))
)
source_data <- data.frame(
  Source = rep(c("Beaver", "Deer"), each = 3),
  d13C = c(-25, -24, -26, -18, -17, -19),
  d15N = c(5, 6, 4, 8, 9, 7)
)
tdf_data <- data.frame(
  Source = c("Beaver", "Deer"),
  d13C_mean = c(1, 1.2), d13C_sd = c(0.2, 0.3),
  d15N_mean = c(3, 3.1), d15N_sd = c(0.4, 0.5)
)
bsimms_get_prior(
  ~ 1 + (1 | Region), mixture_data = mixture_data, source_data = source_data,
  tdf_data = tdf_data, isotope_names = c("d13C", "d15N")
)
}
\references{
Moore, J.W., & Semmens, B.X. (2008). Incorporating
uncertainty and prior information into stable isotope mixing models.
\emph{Ecology Letters}, 11(5), 470-480.
\doi{10.1111/j.1461-0248.2008.01163.x}

Parnell, A.C., Inger, R., Bearhop, S., & Jackson, A.L.
(2010). Source partitioning using stable isotopes: coping with too
much variation. \emph{PLoS ONE}, 5(3), e9672.
\doi{10.1371/journal.pone.0009672}

Stock, B.C., & Semmens, B.X. (2016). Unifying error
structures in commonly used biotracer mixing models. \emph{Ecology},
97(10), 2562-2569. \doi{10.1002/ecy.1517} Appendix S2 simulation-tests
the default \code{"resid_prop"} prior (\code{uniform(0, 20)}) against three
priors centred/moded at 1 (chi-square(3), gamma(2,2), log-normal(0,1))
and finds it better-calibrated at high consumer variance.
}
