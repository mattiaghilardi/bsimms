% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilr.R
\name{clr}
\alias{clr}
\alias{clr_inv}
\title{Centred log-ratio transform and its inverse}
\usage{
clr(x)

clr_inv(y)
}
\arguments{
\item{x}{A vector or matrix of strictly positive compositions. If a
matrix, rows are compositions.}

\item{y}{A vector or matrix on the clr scale (rows are clr vectors if a
matrix).}
}
\value{
\code{clr(x)}, same shape as \code{x}. \code{clr_inv(y)} returns a composition
(or matrix of compositions) on the simplex.
}
\description{
\code{clr()} maps compositions to the (mean-centred) log scale; \code{clr_inv()}
(a softmax) maps back to the simplex.
}
\examples{
p <- c(0.5, 0.3, 0.2)
z <- clr(p)
clr_inv(z)  # back to p
}
