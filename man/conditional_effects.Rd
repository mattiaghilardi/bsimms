% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_effects.R
\name{conditional_effects}
\alias{conditional_effects}
\alias{plot.bsimms_conditional_effects}
\title{Conditional effects of covariates on source proportions or predicted
mixture isotope values}
\usage{
conditional_effects(
  object,
  effects = NULL,
  ref_conditions = NULL,
  int_conditions = NULL,
  resolution = 100,
  re_formula = NA,
  robust = FALSE,
  probs = c(0.5, 0.95),
  point_size = 2,
  method = c("posterior_proportions", "posterior_epred", "posterior_predict"),
  resp = NULL,
  ...
)

\method{plot}{bsimms_conditional_effects}(x, plot = TRUE, ask = TRUE, ...)
}
\arguments{
\item{object}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{effects}{Character vector of fixed-effect covariate name(s) to
vary (variable names in \code{formula}, not dummy-coded column names).
\code{NULL} (default) computes conditional effects for every term of
\code{formula}'s fixed-effect part, main effects and two-way interactions
alike (e.g. \code{~ var1 * var2} gives conditional effects for \code{var1},
\code{var2}, and their interaction \code{var1:var2}); three-way and higher
interactions are dropped from this default, since only two-way
interactions are supported. An entry may name a
two-way interaction as \code{"var1:var2"}: \code{var1} is swept as usual (its
range if numeric, its observed levels if a factor) and becomes the
plot's x axis, while \code{var2} becomes the \emph{moderator} -- the plot is
faceted over \code{var2}'s observed levels (factor) or a small set of
representative values (numeric; see \code{int_conditions}).}

\item{ref_conditions}{Optional named list overriding the default
reference value used to hold a fixed-effect covariate constant while
another varies (any covariate currently neither swept nor used as a
two-way interaction's moderator). Name each element after the
covariate; the value is a single number (for a numeric covariate,
replacing the default mean) or a single level (for a factor
covariate, replacing the default of its first level). A covariate not
named in \code{ref_conditions} uses the default.}

\item{int_conditions}{Optional named list overriding the default
representative values used for a two-way interaction's moderator
(\code{var2} in \code{"var1:var2"}; ignored for single covariates). Name each
element after the moderator variable; the value is a numeric vector
of representative values (for a numeric moderator, replacing the
default mean and mean +/- 1 SD) or a character vector of levels to
include (for a factor moderator, replacing the default of all
observed levels). A moderator not named in \code{int_conditions} uses the
default.}

\item{resolution}{Number of points spanning a numeric covariate's range
(default 100). Ignored for factor covariates, which use their
observed levels.}

\item{re_formula}{Which group-level terms to condition on; see
\code{\link[=posterior_proportions]{posterior_proportions()}}. Defaults to \code{NA} (population-average),
since the prediction grid only ever varies fixed-effect covariates.}

\item{robust}{Logical; if \code{FALSE} (default) the point estimate is the
\code{mean}, if \code{TRUE} the \code{median}.}

\item{probs}{One or more credible-interval masses to display, e.g. the
default \code{c(0.5, 0.95)} draws both a 50\% and a 95\% interval (nested,
narrower intervals drawn more prominently).}

\item{point_size}{For factor covariates, the size of the point marking
the central estimate (default 2). Ignored for numeric covariates.}

\item{method}{Which posterior quantity to compute and plot:
\code{"posterior_proportions"} (default, via \code{\link[=posterior_proportions]{posterior_proportions()}}),
\code{"posterior_epred"} (expected mixture isotope value, via
\code{\link[=posterior_epred.bsimms_fit]{posterior_epred.bsimms_fit()}}), or \code{"posterior_predict"} (posterior
predictive mixture isotope value, via
\code{\link[=posterior_predict.bsimms_fit]{posterior_predict.bsimms_fit()}}); the latter two require
\code{rstantools}.}

\item{resp}{For \code{method = "posterior_epred"}/\code{"posterior_predict"},
the isotope to plot (one of \code{isotope_names}); required if the model
has more than one isotope, since isotopes are shown one at a time
rather than together (unlike sources, they don't share a common
scale). Defaults to the only isotope otherwise. Ignored for
\code{method = "posterior_proportions"}.}

\item{...}{For \code{conditional_effects()}, further arguments passed to
the underlying \code{method} function, e.g. \code{ndraws}. For the \code{plot()}
method, further arguments passed to \code{\link[ggplot2:geom_ribbon]{ggplot2::geom_ribbon()}}
(numeric covariates) or \code{\link[ggplot2:geom_linerange]{ggplot2::geom_linerange()}} (factor
covariates).}

\item{x}{An object returned by \code{conditional_effects()}.}

\item{plot}{For the \code{plot()} method, logical: display each plot as a
side effect (default \code{TRUE})? If \code{FALSE}, the plots are only built
and returned.}

\item{ask}{For the \code{plot()} method, logical: prompt the user before
displaying each new plot after the first (default \code{TRUE})? Only
relevant if \code{plot = TRUE} and there is more than one effect.}
}
\value{
\code{conditional_effects()} returns an object of class
\code{bsimms_conditional_effects}: a list of data frames, one per
covariate/interaction in \code{effects}, each with one row per (grid
point, category, interval width) and columns \code{row}, \code{source}/
\code{isotope} (depending on \code{method}), \code{estimate}, \code{lower}, \code{upper},
\code{width}, the covariate's values, and (for a two-way interaction) the
moderator's values. The \code{plot()} method returns a list of \code{ggplot}
objects (one per covariate), invisibly; each is also displayed as a
side effect.
}
\description{
Computes posterior source proportions (\code{method = "posterior_proportions"},
the default) or expected/predicted mixture isotope values (\code{method = "posterior_epred"}/\code{"posterior_predict"}) across the range (for a numeric
covariate) or levels (for a factor covariate) of one or two
fixed-effect covariates from \code{formula}, holding every other
fixed-effect covariate at a reference value (the mean, for numeric
covariates; the first level, for factors -- overridable via
\code{ref_conditions}) and every group-level (random-effect) term at the
population-average level
(\code{re_formula = NA} by default, since the grid never varies grouping
columns). Printing or \code{\link[=plot]{plot()}}-ing the result (requires \code{ggplot2})
draws one plot per requested effect: a ribbon (numeric covariate) or
forest-plot-style linerange (factor covariate), with one or more
nested credible intervals (narrower drawn more prominently), faceted
by the second covariate when a two-way interaction is requested. For
\code{method = "posterior_proportions"}, every source is shown together on
one plot (colour/fill); for \code{"posterior_epred"}/\code{"posterior_predict"},
isotopes are shown one at a time instead (see \code{resp}), since isotopes
don't share a common scale the way proportions do.
}
\examples{
\donttest{
sim <- simulate_bsimms_data(
  ~Sex,
  n_mixture_obs = 10,
  source_names = c("Beaver", "Deer", "Hare"),
  isotope_names = c("d13C", "d15N"),
  n_levels = list(Sex = 2),
  seed = 1
)
fit <- bsimm(
  sim$formula, mixture_data = sim$mixture_data,
  source_data = sim$source_data, tdf_data = sim$tdf_data,
  isotope_names = sim$isotope_names,
  source_means_sds = sim$source_means_sds, tdf_means_sds = sim$tdf_means_sds,
  conc_dep = sim$conc_dep, error_structure = sim$error_structure,
  source_col = sim$source_col,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
ce <- conditional_effects(fit)
plot(ce)
}
}
