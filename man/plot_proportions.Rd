% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_proportions}
\alias{plot_proportions}
\title{Plot posterior source proportions}
\usage{
plot_proportions(
  p_arr,
  type = c("density", "histogram", "interval"),
  probs = c(0.5, 0.95),
  robust = FALSE,
  point_size = 2,
  ...
)
}
\arguments{
\item{p_arr}{A numeric \verb{[n_draws, n_obs, K]} array of posterior
proportion draws, with source names attached as the 3rd dimension's
\code{dimnames}, as returned by \code{\link[=posterior_proportions]{posterior_proportions()}}.}

\item{type}{Character; \code{"density"} (default), \code{"histogram"}, or
\code{"interval"}. \code{"density"}/\code{"histogram"} require \code{p_arr} to have
exactly one observation (row); use \code{"interval"} for more than one.}

\item{probs}{One or more credible-interval masses to display when
\code{type = "interval"}, e.g. the default \code{c(0.5, 0.95)} draws both a 50\%
and a 95\% interval. Ignored for \code{"density"}/\code{"histogram"}.}

\item{robust}{Logical; if \code{FALSE} (default) the point estimate (for
\code{type = "interval"}) is the \code{mean}, if \code{TRUE} the \code{median}. Ignored
for \code{"density"}/\code{"histogram"}.}

\item{point_size}{Size of the point marking the central estimate, for
\code{type = "interval"} (default 2). Ignored otherwise.}

\item{...}{Further arguments passed to the underlying \code{ggplot2} geom:
\code{\link[ggplot2:geom_density]{ggplot2::geom_density()}}, \code{\link[ggplot2:geom_histogram]{ggplot2::geom_histogram()}}, or
\code{\link[ggplot2:geom_linerange]{ggplot2::geom_linerange()}} for \code{type} \code{"density"}, \code{"histogram"}, or
\code{"interval"} respectively.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots posterior source proportions, as returned by
\code{\link[=posterior_proportions]{posterior_proportions()}} or \code{\link[=fitted_proportions]{fitted_proportions()}} (\code{summary = FALSE}): a density or histogram of the posterior distribution
(\code{type = "density"}/\code{"histogram"}, one observation only), or nested
credible intervals across one or more observations (\code{type = "interval"}, a forest/caterpillar plot coloured by source). Requires
\code{ggplot2}.
}
\examples{
\donttest{
sim <- simulate_bsimms_data(
  ~1,
  n_mixture_obs = 10,
  source_names = c("Beaver", "Deer", "Hare"),
  isotope_names = c("d13C", "d15N"),
  seed = 1
)
fit <- bsimm(
  sim$formula, mixture_data = sim$mixture_data,
  source_data = sim$source_data, tdf_data = sim$tdf_data,
  isotope_names = sim$isotope_names,
  source_means_sds = sim$source_means_sds, tdf_means_sds = sim$tdf_means_sds,
  conc_dep = sim$conc_dep, error_structure = sim$error_structure,
  source_col = sim$source_col,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
p_arr <- posterior_proportions(fit)
plot_proportions(p_arr, type = "interval")
plot_proportions(p_arr[, 1, , drop = FALSE], type = "density")
}
}
