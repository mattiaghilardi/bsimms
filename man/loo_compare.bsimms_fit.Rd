% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{loo_compare.bsimms_fit}
\alias{loo_compare.bsimms_fit}
\title{Compare \code{bsimms} models by approximate leave-one-out cross-validation}
\usage{
\method{loo_compare}{bsimms_fit}(x, ..., criterion = c("loo", "waic"), model_names = NULL)
}
\arguments{
\item{x}{A \code{bsimms_fit} object. Must be a \code{bsimms_fit} (not a
precomputed \code{loo}/\code{waic} object) for this method to be dispatched to;
a precomputed \code{loo}/\code{waic} object can only appear in \code{...}.}

\item{...}{Additional \code{bsimms_fit} objects and/or \code{loo}/\code{waic} objects to
compare against \code{x}.}

\item{criterion}{\code{"loo"} (default) or \code{"waic"}; which criterion to use
for any \code{bsimms_fit} objects passed in (cached via \code{\link[=add_criterion]{add_criterion()}}
if present, computed on the fly otherwise). Ignored for arguments that
are already \code{loo}/\code{waic} objects.}

\item{model_names}{Optional character vector naming the compared models,
in the order \code{x}, \code{...}. Defaults to the deparsed argument expressions.}
}
\value{
The result of \code{\link[loo:loo_compare]{loo::loo_compare()}}: model names are given in its
\code{model} column, ranked by expected log pointwise predictive density
(best model first).
}
\description{
Computes \code{\link[loo:loo]{loo::loo()}} or \code{\link[loo:waic]{loo::waic()}} for each model (a \code{loo}/\code{waic}
object passed directly, e.g. one already computed via \code{loo(fit)}, is used
as-is; for a \code{bsimms_fit}, a criterion already cached via
\code{\link[=add_criterion]{add_criterion()}} is reused instead of recomputed) and ranks them with
\code{\link[loo:loo_compare]{loo::loo_compare()}}.
}
\examples{
\donttest{
sim <- simulate_bsimms_data(
  ~Sex,
  n_mixture_obs = 10,
  source_names = c("Beaver", "Deer", "Hare"),
  isotope_names = c("d13C", "d15N"),
  n_levels = list(Sex = 2),
  seed = 1
)
fit1 <- bsimm(
  ~1, mixture_data = sim$mixture_data,
  source_data = sim$source_data, tdf_data = sim$tdf_data,
  isotope_names = sim$isotope_names,
  source_means_sds = sim$source_means_sds, tdf_means_sds = sim$tdf_means_sds,
  conc_dep = sim$conc_dep, error_structure = sim$error_structure,
  source_col = sim$source_col,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
fit2 <- bsimm(
  ~Sex, mixture_data = sim$mixture_data,
  source_data = sim$source_data, tdf_data = sim$tdf_data,
  isotope_names = sim$isotope_names,
  source_means_sds = sim$source_means_sds, tdf_means_sds = sim$tdf_means_sds,
  conc_dep = sim$conc_dep, error_structure = sim$error_structure,
  source_col = sim$source_col,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
loo::loo_compare(fit1, fit2)
}
}
