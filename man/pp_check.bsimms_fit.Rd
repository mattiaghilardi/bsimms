% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{pp_check.bsimms_fit}
\alias{pp_check.bsimms_fit}
\title{Posterior predictive checks}
\usage{
\method{pp_check}{bsimms_fit}(object, resp = NULL, type = "dens_overlay", ndraws = NULL, ...)
}
\arguments{
\item{object}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{resp}{Character; the isotope to plot (one of \code{isotope_names}).
Required if the model has more than one isotope; defaults to the only
isotope otherwise.}

\item{type}{Character; the \code{bayesplot} PPC plot type, e.g.
\code{"dens_overlay"} (default), \code{"hist"}, \code{"stat"}, \code{"scatter_avg"},
\code{"intervals"} — see \code{\link[bayesplot:available_ppc]{bayesplot::available_ppc()}} for the full list
(passed without its \code{"ppc_"} prefix).}

\item{ndraws}{Optional integer; number of \code{y_rep} draws to (randomly)
subsample for the plot. \code{NULL} (default) uses every draw for PPC types
that aggregate/summarise across draws (e.g. \code{"stat"}, \code{"intervals"},
\code{"scatter_avg"}), where more draws only improve precision, or 10 draws
for types that overlay one \code{y_rep} dataset per draw (e.g.
\code{"dens_overlay"}, \code{"hist"}), where using every draw would overplot the
figure.}

\item{...}{Further arguments passed on to the underlying \verb{ppc_*}
function, e.g. \code{group} for grouped types.}
}
\value{
A ggplot object, as returned by the underlying \verb{ppc_*} function.
}
\description{
Compares observed mixture isotope values against posterior predictive
draws (\code{y_rep}) using \code{\link[bayesplot:pp_check]{bayesplot::pp_check()}}'s \verb{ppc_*} plotting
functions. Since these expect a single response vector, one isotope must
be selected.
}
