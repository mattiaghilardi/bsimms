% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilr.R
\name{ilr_basis}
\alias{ilr_basis}
\title{Isometric log-ratio (ILR) basis matrix}
\usage{
ilr_basis(K)
}
\arguments{
\item{K}{Integer, number of parts (sources), \code{K >= 2}.}
}
\value{
A numeric matrix with \code{K} rows and \code{K - 1} columns. Columns are
orthonormal and each column sums to zero, so \code{V \%*\% z} always lands on
the zero-sum (clr) hyperplane for any \code{z}.
}
\description{
Builds the default orthonormal ILR basis of Egozcue et al. (2003,
eq. 17-18) for a composition with \code{K} parts: column \code{i} is the vector
\deqn{u_i = \sqrt{i/(i+1)} \, [\underbrace{1/i, \dots, 1/i}_{i}, -1, 0,
\dots, 0]}
whose \code{clr}-inverse, \code{e_i = clr_inv(u_i)}, is the \code{i}-th element of the
Aitchison-orthonormal simplex basis (eq. 18) contrasting the (equally
weighted) average of the first \code{i} parts against part \code{i + 1} — the
same default sequential basis used by \code{MixSIAR}. \code{bsimms} passes this
matrix into the generated Stan program as data (\code{V}), and recovers
source proportions from an unconstrained linear predictor \code{eta} (in ILR
space) via \code{softmax(V * eta)} (see \code{\link[=ilr_inv]{ilr_inv()}}).
}
\examples{
V <- ilr_basis(4)
round(crossprod(V), 10)   # identity: columns are orthonormal
round(colSums(V), 10)     # zero: valid clr-constrained basis
}
\references{
Egozcue, J.J., Pawlowsky-Glahn, V., Mateu-Figueras, G., &
Barcelo-Vidal, C. (2003). Isometric logratio transformations for
compositional data analysis. \emph{Mathematical Geology}, 35(3), 279-300.
\doi{10.1023/A:1023818214614}
}
