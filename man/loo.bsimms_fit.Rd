% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{loo.bsimms_fit}
\alias{loo.bsimms_fit}
\title{Approximate leave-one-out cross-validation}
\usage{
\method{loo}{bsimms_fit}(x, cores = getOption("mc.cores", 1), ...)
}
\arguments{
\item{x}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{cores}{Number of cores used for \code{\link[loo:relative_eff]{loo::relative_eff()}} and
\code{\link[loo:loo]{loo::loo()}} (default \code{getOption("mc.cores", 1)}).}

\item{...}{Further arguments passed on to \code{\link[loo:loo.array]{loo::loo.array()}}.}
}
\value{
A \code{loo} object, as returned by \code{\link[loo:loo]{loo::loo()}}.
}
\description{
Computes PSIS-LOO (Vehtari, Gelman, and Gabry 2017) from the model's
\code{log_lik} generated quantity, which is the joint log density of each
mixture sample's full isotope profile (isotopes are summed/modelled
jointly per mixture sample, never held out individually, so the mixture
sample is the leave-one-out unit -- see \code{\link[=make_stancode]{make_stancode()}}).
}
\references{
Vehtari, A., Gelman, A., & Gabry, J. (2017). Practical
Bayesian model evaluation using leave-one-out cross-validation and WAIC.
\emph{Statistics and Computing}, 27(5), 1413-1432.
\doi{10.1007/s11222-016-9696-4}
}
