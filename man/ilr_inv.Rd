% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ilr.R
\name{ilr_inv}
\alias{ilr_inv}
\title{Inverse ILR transform: ILR coordinates to proportions}
\usage{
ilr_inv(z, V = NULL)
}
\arguments{
\item{z}{A vector of length \code{K - 1}, or an \verb{N x (K - 1)} matrix of ILR
coordinates.}

\item{V}{Optional ILR basis from \code{\link[=ilr_basis]{ilr_basis()}}. If \code{NULL}, computed
automatically from \code{ncol(z) + 1} / \code{length(z) + 1}.}
}
\value{
Proportions on the \code{K}-part simplex: a vector of length \code{K}, or
an \verb{N x K} matrix.
}
\description{
Implements the inverse isometric log-ratio transformation of Egozcue
et al. (2003, eq. 24), \eqn{x = \bigoplus_{i} (y_i \otimes e_i)}: each
ILR coordinate \code{z[d]} perturbation-scales its simplex-domain basis
element \code{e_d = clr_inv(V[, d])} (a power operation followed by
closure/normalisation), and the results are combined by repeated
perturbation (elementwise product followed by closure). This is the
same construction used by \code{MixSIAR}'s JAGS implementation, and is
what \code{bsimms}'s generated Stan code uses internally (the \code{inverse_ilr()}
function in the \code{functions} block of \code{make_stancode()}'s output) to map
the ILR-scale linear predictor back onto the source simplex.
}
\references{
Egozcue, J.J., Pawlowsky-Glahn, V., Mateu-Figueras, G., &
Barcelo-Vidal, C. (2003). Isometric logratio transformations for
compositional data analysis. \emph{Mathematical Geology}, 35(3), 279-300.
\doi{10.1023/A:1023818214614}
}
