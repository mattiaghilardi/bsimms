% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws.R
\name{bsimms_draws}
\alias{bsimms_draws}
\title{Extract posterior draws from a \code{bsimms} fit}
\usage{
bsimms_draws(object, variable = NULL)
}
\arguments{
\item{object}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{variable}{Optional character vector of variable names (or
\code{posterior}-style selectors) to extract; \code{NULL} extracts everything.}
}
\value{
A \code{posterior::draws_array}.
}
\description{
Backend-agnostic accessor returning a \code{posterior::draws_array} regardless
of whether the model was fit with \code{cmdstanr} or \code{rstan}.
}
\examples{
\donttest{
mixture_data <- data.frame(
  d13C = c(-20, -21, -19, -22, -20.5, -21.5),
  d15N = c(10, 11, 9, 12, 10.5, 11.5),
  Region = factor(rep(c("A", "B"), each = 3))
)
source_data <- data.frame(
  Source = rep(c("Beaver", "Deer"), each = 3),
  d13C = c(-25, -24, -26, -18, -17, -19),
  d15N = c(5, 6, 4, 8, 9, 7)
)
tdf_data <- data.frame(
  Source = c("Beaver", "Deer"),
  d13C_mean = c(1, 1.2), d13C_sd = c(0.2, 0.3),
  d15N_mean = c(3, 3.1), d15N_sd = c(0.4, 0.5)
)
fit <- bsimm(
  ~ 1 + (1 | Region), mixture_data = mixture_data, source_data = source_data,
  tdf_data = tdf_data, isotope_names = c("d13C", "d15N"),
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
bsimms_draws(fit, variable = "p_global")
}
}
