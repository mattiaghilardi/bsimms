% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{parse_bsimms_formula}
\alias{parse_bsimms_formula}
\title{Parse an \code{lme4}-style formula for the source-proportion model}
\usage{
parse_bsimms_formula(formula, data)
}
\arguments{
\item{formula}{A one-sided formula, e.g. \code{~ Sex + Season + (1 | Region)}.}

\item{data}{The mixture data frame.}
}
\value{
A list with elements \code{formula}, \code{fixed_formula}, \code{X} (fixed-effect
design matrix), \code{fixed_names}, \code{fixed_frame} (model frame of the
fixed-effect covariates, one column per named variable, character
columns coerced to factor), and \code{re_terms} (a list, one element per
group-level term, each with \code{group}, \code{term_names}, \code{Z}, \code{group_idx},
\code{group_levels}).
}
\description{
\code{bsimms} formulas describe how the (ILR-transformed) source proportions
depend on mixture-level covariates, using the same syntax as \code{lme4}
(parsed here via the \code{reformulas} package, which now houses this
formula-processing machinery upstream of \code{lme4}/\code{glmmTMB}): fixed-effect
terms as usual (\code{~ Sex + Season}), and group-level ("random-effect")
terms in parentheses with a bar (\code{(1 | Region)}, \code{(1 + Season | Individual)}, \code{(x || Region)} for an uncorrelated slope and intercept).
Formulas must not have a left-hand side: the response (source
proportions) is never observed directly, and is instead inferred from
the isotope mixture likelihood.
}
\details{
All variables referenced in \code{formula} must be columns of the \emph{mixture}
data set (source proportions describe mixture samples, and covariates
such as sex, age class, season, or capture site are properties of the
mixture sample, not the source).

There is no limit on the number or type of fixed-effect covariates
(continuous, factor, interactions, ...) and no limit on
the number of independent, crossed, or nested group-level (hierarchical)
terms: anything \code{lme4::lmer()} could parse on the right-hand side of a
formula is supported here, including \code{/} for nested terms (e.g.
\code{(1 | site/individual)}, expanded exactly as in \code{lme4} into
\code{(1 | site) + (1 | individual:site)}) and \code{:} for crossed terms (e.g.
\code{(1 | site:individual)}).
}
\examples{
d <- data.frame(Sex = c("M", "F", "F", "M"), Region = c("A", "A", "B", "B"))
pf <- parse_bsimms_formula(~ Sex + (1 | Region), data = d)
pf$fixed_names
pf$re_terms[[1]]$group
}
