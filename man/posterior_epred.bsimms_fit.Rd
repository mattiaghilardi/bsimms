% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{posterior_epred.bsimms_fit}
\alias{posterior_epred.bsimms_fit}
\title{Expected mixture isotope values}
\usage{
\method{posterior_epred}{bsimms_fit}(
  object,
  newdata = NULL,
  resp = NULL,
  re_formula = NULL,
  allow_new_levels = FALSE,
  sample_new_levels = c("uncertainty", "gaussian"),
  ndraws = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{newdata}{Optional data frame of new mixture covariate values (same
columns as used in \code{formula}).}

\item{resp}{Character vector of isotope name(s) (from \code{isotope_names})
to return. \code{NULL} (default) returns all isotopes.}

\item{re_formula}{Which group-level (random-effect) terms to condition on,
whether predicting for the fitted mixture samples (\code{newdata = NULL}) or
for new ones: \code{NULL} (default) conditions on every group-level term in
the fitted model (with \code{newdata}, every term's grouping column(s) must
then be supplied); \code{NA} or \code{~0} conditions on none of them
(population-average for every term, regardless of what \code{newdata}
contains); a reduced formula naming a subset, e.g. \code{~ (1 | Site)},
conditions only on the named term(s) (with \code{newdata}, only those
term(s)' columns are needed). When \code{newdata} is \code{NULL},
\code{allow_new_levels}/\code{sample_new_levels} are ignored, since a fitted
mixture sample's group membership is never new.}

\item{allow_new_levels}{Logical; only relevant with \code{newdata}. If \code{FALSE}
(default), a grouping column value not seen when fitting (including
\code{NA}, which is always treated as a new level) raises an error. If
\code{TRUE}, a posterior draw is instead sampled for that new level via
\code{sample_new_levels}; every row sharing the same new, non-\code{NA} level
value gets the same sampled draws (as for a real level), while each
\code{NA} row is sampled independently (it asserts no shared identity).}

\item{sample_new_levels}{How to sample a new level's group-level deviation
when \code{allow_new_levels = TRUE}: \code{"uncertainty"} (default) draws, for
each posterior draw, from a randomly chosen \emph{existing} level's draw at
that same iteration -- most appropriate with many existing levels,
where this empirically reflects the observed between-level spread;
\code{"gaussian"} instead draws a fresh value for the new level from a
normal distribution centred at zero, using that draw's own estimated
group-level SD (and correlation, for multi-column terms) -- more
appropriate with few existing levels, where \code{"uncertainty"} could only
ever resample one of a handful of specific levels rather than
represent a genuinely new one.}

\item{ndraws}{Number of posterior draws to use, randomly subset from the
full posterior. \code{NULL} (default) uses all draws.}

\item{...}{Currently unused.}
}
\value{
A numeric \verb{[n_draws, n_obs, length(resp)]} array, with isotope
names attached as the 3rd dimension's \code{dimnames}.
}
\description{
Returns raw posterior draws of the model's expected mixture isotope
value \code{mu} (the mixing-model prediction \emph{before} observation error is
added), either for the fitted mixture samples (\code{newdata = NULL}) or for
new mixture covariate combinations. Use \code{\link[=fitted.bsimms_fit]{fitted.bsimms_fit()}} for a
summarised version instead, \code{\link[=posterior_proportions]{posterior_proportions()}} for the underlying
source proportions, or \code{\link[=posterior_predict.bsimms_fit]{posterior_predict.bsimms_fit()}} for posterior
predictive draws that include observation error.
}
\examples{
\donttest{
sim <- simulate_bsimms_data(
  ~1,
  n_mixture_obs = 10,
  source_names = c("Beaver", "Deer", "Hare"),
  isotope_names = c("d13C", "d15N"),
  seed = 1
)
fit <- bsimm(
  sim$formula, mixture_data = sim$mixture_data,
  source_data = sim$source_data, tdf_data = sim$tdf_data,
  isotope_names = sim$isotope_names,
  source_means_sds = sim$source_means_sds, tdf_means_sds = sim$tdf_means_sds,
  conc_dep = sim$conc_dep, error_structure = sim$error_structure,
  source_col = sim$source_col,
  chains = 2, iter_warmup = 500, iter_sampling = 500
)
mu_arr <- rstantools::posterior_epred(fit)
dim(mu_arr)
}
}
