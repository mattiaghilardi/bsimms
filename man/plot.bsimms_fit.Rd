% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.bsimms_fit}
\alias{plot.bsimms_fit}
\title{Trace and density plots of model parameters}
\usage{
\method{plot}{bsimms_fit}(
  x,
  variable = NULL,
  combo = c("dens", "trace"),
  nvariables = 5,
  plot = TRUE,
  ask = TRUE,
  newpage = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{bsimms_fit} object (as returned by \code{\link[=bsimm]{bsimm()}}).}

\item{variable}{Optional character vector of parameter (base) names to
plot. \code{NULL} (default) plots \code{p_global}, the fixed effects (if any),
group-level standard deviations (if any), and the error term(s).}

\item{combo}{Character vector of two \code{bayesplot} \verb{mcmc_*} plot types to
combine (default \code{c("dens", "trace")}); see \code{\link[bayesplot:mcmc_combo]{bayesplot::mcmc_combo()}}.}

\item{nvariables}{Maximum number of parameters shown per plot; models
with more are split across multiple plots (default 5).}

\item{plot}{Logical; display each plot as a side effect (default
\code{TRUE})? If \code{FALSE}, the plots are only built and returned.}

\item{ask}{Logical; prompt the user before displaying each new page
after the first (default \code{TRUE})? Only relevant if \code{plot = TRUE} and
there is more than one page.}

\item{newpage}{Logical; start the first page on a new graphics page
(default \code{TRUE})? Every page after the first always starts on a new
page regardless. Only relevant if \code{plot = TRUE}.}

\item{...}{Further arguments passed to \code{\link[bayesplot:mcmc_combo]{bayesplot::mcmc_combo()}}.}
}
\value{
A list of plot objects (one per page), invisibly; each is also
displayed as a side effect if \code{plot = TRUE}.
}
\description{
Plots the posterior density and MCMC trace of the model's underlying
Stan parameters (population-average source proportions \code{p_global},
fixed-effect coefficients, group-level standard deviations, and error
term(s)), via \code{\link[bayesplot:mcmc_combo]{bayesplot::mcmc_combo()}}. Use \code{\link[=plot_proportions]{plot_proportions()}} for
summaries of the source proportions themselves.
}
